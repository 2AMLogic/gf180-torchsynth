{
  "schema": "torchsynth-mutation-seams",
  "schema_version": 1,
  "identity": "mutation-seams-v1",
  "summary": "Capability overlay naming apparatus seams where a declared, registered mutation may replace values. This is not a second trace registry: it never redefines #22 trace meanings, order or schemas, and it grants nothing to producers. Adding a writable seam requires a producer handoff that names the landed downstream refusal.",
  "registry_binding": {
    "trace_registry": "spec/reference/trace-registry-v1.json",
    "trace_ownership": "#22/#23 remain the sole owners of trace meaning and passive capture; observers stay passive"
  },
  "seams": {
    "apparatus.producer_call": {
      "phase": "producer",
      "writable": true,
      "summary": "The apparatus fixture producer invocation that yields attempt payload bytes. A declared mutation may raise the identified ProducerCrash or return a replacement value; the Voice graph is never invoked here.",
      "downstream": "injection session in-band errored event; scorecard.make_report / case_registry.row_outcome refuse any PASS from an errored attempt"
    },
    "apparatus.artifact_write": {
      "phase": "persistence",
      "writable": true,
      "summary": "The apparatus write step that persists attempt payload bytes and returns the stored path/size/digest binding. A declared mutation may truncate or bit-flip the stored bytes so they diverge from the declared binding.",
      "downstream": "artifacts.verify_sha256 refuses the stored bytes against the declared receipt digest"
    },
    "apparatus.receipt_append": {
      "phase": "persistence",
      "writable": true,
      "summary": "The apparatus step that emits the scorecard-shaped attempt receipt. A declared mutation may rebind the receipt digest to foreign bytes or drop the receipt from the attempt inventory.",
      "downstream": "scorecard.validate_row refuses malformed receipts; mutations.require_complete_inventory refuses a shrunken denominator; artifacts.verify_sha256 refuses rebound digests"
    },
    "apparatus.evidence_digest": {
      "phase": "persistence",
      "writable": false,
      "summary": "Derived digest binding over already-produced bytes. This is a passive, derived observation and therefore not an executable injection seam; a plan declaring a mutation here must fail closed at registration.",
      "downstream": "mutations.validate_plan refuses any operator at a non-writable seam"
    },
    "board.partition_access": {
      "phase": "access",
      "writable": true,
      "summary": "The partition claim presented to the case registry access gate. A declared mutation may relabel the claim so the holdout refusal path is exercised as a test, never to actually read sealed payloads.",
      "downstream": "case_registry._access refuses before any stat/read of holdout payloads; evaluate refuses unassigned partitions"
    },
    "capture.inventory": {
      "phase": "capture",
      "writable": true,
      "summary": "The capture descriptor inventory presented to the landed capture validator. A declared mutation may swap or drop names so the capture refusal path is exercised against the #22 registry; passive observers themselves stay passive.",
      "downstream": "trace_capture.validate_selected_capture refuses missing, extra, swapped or reordered capture inventories"
    },
    "voice.post_module": {
      "phase": "voice-runtime",
      "writable": true,
      "summary": "In-place replacement of one declared batch slot of one registry-named module output during an actual Voice render: the same tensor object keeps flowing to consumers (preserving the #22 CallTracker producer->consumer argument association) while its declared slot data is replaced after the #23 passive observer has snapshotted the original bytes at the seam. Downstream modules and the returned audio consume the replacement; the captured inventory keeps the original. Occurrence/call association comes from the #22 registry producer binding, never from registration order. Covers module-output classes that downstream families own (noise input, oscillator, gain, timing, envelope, mixer framing); the bridge.* operators are test-only proofs, never family operators.",
      "downstream": "#23 capture inventory holds the original bytes at the seam while downstream traces and returned audio show the replacement; trace_registry.CallTracker argument association and invocation counts stay unchanged; session cleanup restores byte-identical rerenders"
    },
    "voice.parameter_value": {
      "phase": "voice-runtime",
      "writable": true,
      "summary": "Scoped by-reference replacement of one declared batch slot of one inventory-named parameter value, hosted at the keyboard anchor seam (the first registry call): the pinned Voice.forward re-randomizes every parameter at the start of each render, so the swap is applied only after the producer's own initialization completes and before any later module read. The keyboard module's own reads happen before the anchor returns and are therefore not swappable here; swapping them requires a producer handoff. Restored after the attempt.",
      "downstream": "the anchor event records the post-randomization original and the replacement slot digests; module-output traces and returned audio show the replacement; sibling parameters and a post-attempt rerun stay byte-identical to the pristine baseline"
    },
    "voice.normalization_decision": {
      "phase": "voice-runtime",
      "writable": false,
      "summary": "The peak/divide decision inside torchsynth.util.normalize_if_clipping as invoked from AudioMixer.output. Not writable: the decision operand is internal to the producer function, there is no observer/replacement hook at this exact site, and the captured mixer.peak and derived mixer.gain are strictly passive observations that never replace the original division. Producer handoff required: an AudioMixer.output seam that presents the candidate peak and clamp decision to a scoped mutator while the original computation still runs. A plan declaring a mutation here fails closed naming this handoff.",
      "downstream": "mutations.validate_plan refuses with the named producer handoff; mixer.peak/mixer.gain remain captured diagnostics of the original decision"
    }
  }
}
