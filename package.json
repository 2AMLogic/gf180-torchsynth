{
  "name": "gf180-torchsynth",
  "description": "GF180 hardware implementation of the TorchSynth default Voice",
  "private": true,
  "scripts": {
    "worktree:return": "./.loom/scripts/worktree-return.sh",
    "check:ci": "python3 -m unittest discover -s tests -v && python3 tools/check_contract.py",
    "check:all": "npm run check:ci",
    "test": "python3 -m unittest discover -s tests -v",
    "lint": "python3 -m compileall -q src tests tools"
  }
}
